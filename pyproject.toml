[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "swclib"
version = "0.1"

dependencies = [
  "numpy",
  "anytree",
  "miniball",
  "scipy",
  "networkx",
  "rtree"
  "rasterio"
]

[tool.setuptools.packages.find]
where = ["."]